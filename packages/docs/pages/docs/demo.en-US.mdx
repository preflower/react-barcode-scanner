---
title: React Barcode Scanner Interactive Demo
description: Test React Barcode Scanner in the browser, select barcode formats, change the scan delay, pause scanning and inspect camera errors and detected values.
---

import BarcodeScannerDemo from '../../components/barcode-scanner-demo'

Use the controls below to change the scan interval and supported formats. Camera status, errors and detected values are displayed directly on the page.

<BarcodeScannerDemo locale="en-US" />
