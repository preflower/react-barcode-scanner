---
title: React Barcode Scanner Getting Started
description: Build a browser barcode scanner in React, configure formats and delay, control the torch, isolate multiple scanners and integrate with Next.js.
---
import { Callout } from 'nextra/components'

## Quick Start

```jsx
import React from 'react'
import { BarcodeScanner } from 'react-barcode-scanner'
import 'react-barcode-scanner/polyfill'

export default () => {
  return (
    <BarcodeScanner />
  )
}
```

### Barcode format
`BarcodeScanner` supports `qr_code` by default. Use the `formats` option to scan other barcode formats:

```tsx
<BarcodeScanner options={{ formats: ['code_128'] }} /> 
```

<Callout type="warning" emoji="⚠️">
  The scanner is based on the `Barcode Detection API`. The zbar polyfill supports the listed [barcode formats](https://developer.mozilla.org/en-US/docs/Web/API/Barcode_Detection_API#supported_barcode_formats) except `aztec`, `data_matrix` and `pdf417`.
</Callout>

### Scanning delay
For performance, `BarcodeScanner` scans every `1000ms` by default. Use the `delay` option for a shorter or longer interval:

```tsx
<BarcodeScanner options={{ delay: 500 }} />
```

### Torch
Use the `useTorch` hook to inspect support and control the camera torch:

```jsx
import React from 'react'
import { BarcodeScanner, useTorch } from 'react-barcode-scanner'

export default () => {
  const { isTorchSupported, isTorchOn, setIsTorchOn } = useTorch()

  const onTorchSwitch = () => {
    setIsTorchOn(!isTorchOn)
  }

  return (
    <div style={{ width: '100%', height: '360px' }}>
      <BarcodeScanner />
      {isTorchSupported
        ? <button onClick={onTorchSwitch}>Switch Torch</button>
        : null}
    </div>
  )
}
```

> Torch support depends on the browser and device. See [MediaTrackConstraints](https://developer.mozilla.org/en-US/docs/Web/API/MediaTrackConstraints#torch).

### State scope and multiple scanners

For complete ownership, lifecycle, torch-operation and SSR rules, see [State Scope and Multiple Scanners](./state-scope).

Without a Provider, `BarcodeScanner`, `useStreamState()` and `useTorch()` use a global compatibility scope. This keeps the simple single-scanner usage working.

For multiple active scanners, wrap each scanner and its controls in a separate `BarcodeScannerProvider`:

```tsx
import {
  BarcodeScanner,
  BarcodeScannerProvider,
  useStreamState,
  useTorch
} from 'react-barcode-scanner'

function ScannerControls () {
  const [stream] = useStreamState()
  const { isTorchOn, setIsTorchOn } = useTorch()

  return (
    <section>
      <p>Stream: {stream?.id ?? 'none'}</p>
      <button onClick={() => setIsTorchOn(!isTorchOn)}>
        Torch: {isTorchOn ? 'on' : 'off'}
      </button>
    </section>
  )
}

export default function Scanners () {
  return (
    <>
      <BarcodeScannerProvider>
        <BarcodeScanner />
        <ScannerControls />
      </BarcodeScannerProvider>

      <BarcodeScannerProvider initialTorchOn>
        <BarcodeScanner />
        <ScannerControls />
      </BarcodeScannerProvider>
    </>
  )
}
```

`initialTorchOn` is read only when that Provider store is created. Changing the prop later does not overwrite user state.

## With framework

### Next.js

The package can be imported during SSR, but the scanner itself uses browser APIs after mounting. Keep the Provider, scanner and external state consumers in the same Client Component:

```tsx filename="ScannerPanel.tsx"
'use client'

import {
  BarcodeScanner,
  BarcodeScannerProvider
} from 'react-barcode-scanner'
import 'react-barcode-scanner/polyfill'

export default function ScannerPanel () {
  return (
    <BarcodeScannerProvider>
      <BarcodeScanner />
      <ScannerControls />
    </BarcodeScannerProvider>
  )
}
```

With the Pages Router, dynamically load the complete panel when you want to disable SSR for the scanner area:

```tsx
import dynamic from 'next/dynamic'

const ScannerPanel = dynamic(
  () => import('../components/ScannerPanel'),
  { ssr: false }
)
```

With the App Router, the component that declares `dynamic(..., { ssr: false })` must itself be a Client Component:

```tsx
'use client'

import dynamic from 'next/dynamic'

const ScannerPanel = dynamic(
  () => import('./ScannerPanel'),
  { ssr: false }
)

export default function ScannerPageClient () {
  return <ScannerPanel />
}
```
